using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Crickets : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
      // Ввод в консоль приветствия миру
      print("crickets");  
    }

    // Update is called once per frame
    void Update()
    {
      Debug.Log("crickets");  
    }
}